name: Black Linting

on:
  pull_request:
    branches:
      - main

permissions:
  checks: write
  contents: write

jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v2

      - name: Set up ${{ matrix.python-version }} Python
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Python dependencies
        run: pip install black mypy && pip install -r ./scripts/requirements.txt

      - name: Install Python Types
        run: mypy --install-types --non-interactive $(git ls-files '*.py') || true

      - name: Run Black Linter
        uses: wearerequired/lint-action@v2
        with:
          auto_fix: true 
          black: true
          mypy: true
          mypy_auto_fix: false